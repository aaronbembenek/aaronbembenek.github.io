<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://aaronbembenek.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://aaronbembenek.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-27T07:20:36+00:00</updated><id>https://aaronbembenek.github.io/feed.xml</id><title type="html">blank</title><subtitle>Computer science postdoc at The University of Melbourne. Research in programming languages, automated formal methods, and binary analysis. </subtitle><entry><title type="html">AI That Is Simultaneously Logical and Intuitive</title><link href="https://aaronbembenek.github.io/blog/2026/ai-that-is-logical-and-intuitive/" rel="alternate" type="text/html" title="AI That Is Simultaneously Logical and Intuitive"/><published>2026-05-27T00:00:00+00:00</published><updated>2026-05-27T00:00:00+00:00</updated><id>https://aaronbembenek.github.io/blog/2026/ai-that-is-logical-and-intuitive</id><content type="html" xml:base="https://aaronbembenek.github.io/blog/2026/ai-that-is-logical-and-intuitive/"><![CDATA[<p>In July, I will begin a three-year McKenzie Postdoctoral Fellowship, which will allow me to pursue an independent research project at The University of Melbourne. This is the first in a series of blog posts documenting my progress on this project.</p> <p>The goal of my project is to develop the foundations for advanced AI systems that are both <em>logical</em> and <em>intuitive</em>. The ability to reason in a logically correct way strikes me as a key desideratum for future AI systems, both from the perspective of AI safety—we want to be able to trust the “thought process” of AI systems—and from a capability perspective, since certain tasks require logical reasoning. After all, it seems reasonable to expect a superintelligent machine to draw correct conclusions from assumptions, satisfy situational constraints, and generally be logically consistent. At the same time, AI systems that are purely logical are inefficient (since they cannot use intuition to reach solutions quickly) and hard to interact with (since they cannot explain themselves to users in an intuitive way). No current AI systems combine logic and intuition in a way that takes full advantage of these complementary modalities—a gap my project aims to address.</p> <p>In this post, I will lay out the context and motivation of my project, as well as its key insight. In later posts, I will explain how this insight can be leveraged to build logical and intuitive AI systems.</p> <h4 id="automated-reasoning-and-symbolic-ai">Automated Reasoning and Symbolic AI</h4> <p>While AI can be used for many tasks, my project focuses on <em>automated reasoning</em>—using computers to solve logical problems. These problems are unambiguous, in that a candidate solution for a problem can be verified in a mechanical way (what is “correct” is not up to someone’s interpretation). Unsurprisingly, traditional automated reasoning tasks have an explicit mathematical flavor; good examples are software verification and automated theorem proving. However, many tasks that are not outwardly mathematical also have facets that are amenable to automated reasoning. Consider the task of writing an argumentative essay: while some aspects of the task are outside the scope of automated reasoning (e.g., the tone of the exposition, which is hard to specify in logic), automated reasoning can ensure that the essay’s argument is logically consistent.</p> <p>Solving an automated reasoning problem typically involves <em>search</em>—exploring multiple paths that might lead to a solution. At certain points in the reasoning process, the only way for the AI system to make progress is to make some working assumptions, which might have to be retracted later if those assumptions result in a dead end. The ability of an automated reasoning tool to find a solution efficiently (or at all) often depends on the quality of the working assumptions it makes during search.</p> <p>Traditionally, automated reasoning tools have been built using <em>symbolic AI</em>, where computation consists of systematically applying rules. To make this more concrete, we can imagine that we have the following twelve rules:</p> <ol> <li>Every mammal is mortal.</li> <li>Every reptile is mortal.</li> <li>Every bird is mortal.</li> <li>Every horse is a mammal.</li> <li>Every dog is a mammal.</li> <li>Every person is a mammal.</li> <li>Every snake is a reptile.</li> <li>Every lizard is a reptile.</li> <li>Every songbird is a bird.</li> <li>Every raptor is a bird.</li> <li>Every penguin is a bird.</li> <li>Socrates is a person.</li> </ol> <p>Given the task of proving “Socrates is mortal”, a symbolic AI system would search for a proof of this proposition, using these rules to establish evidence. The trace of the AI system might look something like this:</p> <ul> <li>I need to prove that Socrates is mortal.</li> <li>To do so, I need to select a rule that defines “mortal”.</li> <li>I chose Rule #1: Every mammal is mortal. Now, I need to prove Socrates is a mammal.</li> <li>To do so, I need to select a rule that defines “mammal”.</li> <li>I chose Rule #6: Every person is a mammal. Now, I need to prove that Socrates is a person.</li> <li>To do so, I need to select a rule that defines “person”.</li> <li>I chose Rule #12: Socrates is a person. This establishes the fact we need.</li> <li>Search complete.</li> </ul> <p>So, the system is able to prove that Socrates is mortal by combining Rules #1, #6, and #12. As we can see here, symbolic AI has the advantage that it is trustworthy: we know that the reasoning process is logically correct, up to the correctness of the rules.</p> <p>However, symbolic AI has two shortcomings that have limited its impact in practice.<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></p> <p>First, it often fails to scale to automated reasoning problems with large search spaces, as it cannot use intuition to more efficiently guide its search for a solution. In our example, a symbolic AI system might try to prove that Socrates is mortal by showing that he is a bird, leading to a dead end, and forcing the system to backtrack:</p> <ul> <li>I need to prove that Socrates is mortal.</li> <li>To do so, I need to select a rule that defines “mortal”.</li> <li>I chose Rule #3: Every bird is mortal. Now, I need to prove Socrates is a bird.</li> <li>To do so, I need to select a rule that defines “bird”.</li> <li>I chose Rule #11: Every penguin is a bird. Now, I need to prove that Socrates is a penguin.</li> <li>To do so, I need to select a rule that defines “penguin”.</li> <li>Dead end: no rule defines penguin. Backtracking…</li> <li>I need to select another rule that defines “bird”.</li> <li>I chose Rule #10: Every raptor is a bird. Now, I need to prove that Socrates is a raptor.</li> <li>…</li> </ul> <p>A human familiar with the history of Western philosophy would not fall into this trap: they would have prior knowledge that the historical Socrates was a person, not a bird (of any type), and so would be suspicious that Rule #3 would be useful for proving Socrates’ mortality. Unfortunately, in contrast, a symbolic AI system does not have the same intuition—after all, in the purely logical world of symbolic AI, terms like “person”, “mortal”, “penguin”, and “Socrates” are just formal symbols with no connection to real-world phenomena. That is, a symbolic AI system has no access to the external meanings of these terms, and thus the terms do not provide any information that can be used to guide search.</p> <p>Second, symbolic AI is difficult to use, as pure logic is hard to understand (at least for most people, myself included), and symbolic AI tools are incapable of explaining their reasoning in an intuitive way. Our Socrates example is small and simple enough that this problem does not manifest, but one could imagine that it becomes tricky for humans to follow the logic—and map it back to the real world—as systems grow to thousands of rules and proofs become commensurately more complex.</p> <h4 id="neurosymbolic-ai-for-automated-reasoning">Neurosymbolic AI for Automated Reasoning</h4> <p>To address these limitations, researchers have become interested in building automated reasoning systems based on <em>neurosymbolic AI</em>—a hybrid paradigm where symbolic AI is combined with neural networks, such as large language models (LLMs). Whereas symbolic AI makes logical inferences based on rules, neural networks make statistical inferences based on learned patterns; these learned patterns can simulate intuition. Indeed, it often appears that an LLM is acting with some form of intuition when we ask it to solve a task. On the other hand, neural networks like LLMs are not guaranteed to act logically (not even in “reasoning” modes), and thus neural networks and LLMs are untrustworthy.</p> <p>The hope is that neurosymbolic AI will enable automated reasoning systems that use “intuition” while staying logically correct—that is, to combine the advantages of neural networks and symbolic AI. How to actually combine symbolic AI and neural networks to achieve the full potential of this combination remains an open question. Indeed, I believe that our current approach to structuring neurosymbolic AI systems limits the impact this exciting paradigm can have on automated reasoning—and, in turn, the potential benefits that automated reasoning can offer society. Now is the time for foundational work in this area!</p> <p>Currently, neurosymbolic automated reasoning systems are conceptualized as consisting of multiple components chained together <em>in sequence</em>, where some components are symbolic, and some are neural networks (typically an LLM). This sequential linking of neural networks and symbolic components is essentially what happens in agentic frameworks, where an AI agent (the neural network) passes control to external tools (symbolic code) that run and then return control back to the agent. A classic example is the guess-and-check loop, where the LLM plays the role of the “guesser”, and the symbolic component plays the role of the “checker”. The LLM guesses a candidate solution to a problem; the symbolic component checks if it is actually a solution; if not, the symbolic component passes a counterexample to the LLM (demonstrating why the candidate is not a solution), and the LLM guesses again. The idea is that, as the counterexamples accumulate, the guesser will be led to a solution.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/sequential_loop.svg" sizes="95vw"/> <img src="/assets/img/sequential_loop.svg" class="mx-auto d-block" width="50%" height="auto" alt="A clockwise loop with two component boxes: on the left, a blue 'Guesser (LLM)' box; on the right, an orange 'Symbolic checker' box. A vertical dashed line between them marks the neural-symbolic boundary. Along the top, the guess flows left to right, appearing first as a blue 'Neural guess' box and then, after crossing the boundary, as an orange 'Symbolic guess' box. Along the bottom, the counterexample flows right to left, appearing first as an orange 'Symbolic counterexample' box and then, after crossing the boundary, as a blue 'Neural counterexample' box." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">A guess-and-check loop in the sequential neurosymbolic architecture, where an implicit neural-symbolic boundary separates the world of intuition (left, blue) from the world of logic (right, orange)</figcaption> </figure> <p>There are two limitations that are fundamental to an architecture like this, where computation flows through neural networks and symbolic components <em>in sequence.</em></p> <p>First, the guesser, being purely neural, comes with no guarantees about its behavior. In particular, there is no guarantee that it will produce candidate solutions that respect the counterexamples it has been given—meaning that, in principle, the LLM might guess the same wrong answers repeatedly, and the loop never makes progress. Such guarantees are standard for symbolic algorithms, but very hard to establish even for small neural networks, let alone frontier language models.</p> <p>Second, the checker, being purely symbolic, does not have any of the advantages of neural networks, which would allow it to operate with “intuition”. For one, it cannot ingest intuition for the candidate solution it receives. This is a loss: if the checker had access to the intuition behind a guess, the checker could produce a counterexample that more effectively addresses why that intuition is incorrect. Moreover, the checker cannot give any intuition when it produces a counterexample; such intuition might help the guesser better understand how the counterexample works, leading to a better subsequent guess.</p> <p>These limitations are inherent in our current (slightly Frankenstein-esque) approach of bolting together symbolic components and neural networks in sequence. My project asks how we can integrate symbolic components and neural networks in a deeper, principled way that will enable us to build more powerful neurosymbolic automated reasoning systems.</p> <h4 id="my-insight-intuition-and-logic-should-evolve-in-parallel">My Insight: Intuition and Logic Should Evolve in Parallel</h4> <p>In current conceptualizations, logical computation (through symbolic components) and computation over “intuition” (through neural networks) happen in sequence. My insight is that, instead, these two types of computation should happen <em>in parallel.</em> In this framing, every piece of data simultaneously has a logical facet and an intuitive facet: an automated reasoning problem, for instance, is both a formal logical object and something that represents some external phenomenon (e.g., a real-world situation).</p> <p>During a parallel neurosymbolic computation, the logical and intuitive facets of the computation’s state evolve in lockstep. The logical part evolves through symbolic rules, guaranteeing the overall computation is trustworthy. Each logical step is mirrored by a corresponding update to the intuition state, forming a running summary of the reasoning so far. When search reaches a decision point, this accumulated intuition can be used to guide logical reasoning toward the most likely path. The output is then a solution, determined by the symbolic rules, together with the intuition built up over the course of the computation.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/parallel_nesy.svg" sizes="95vw"/> <img src="/assets/img/parallel_nesy.svg" class="mx-auto d-block" width="50%" height="auto" alt="Three boxes in a left-to-right row connected by arrows, each split into an orange upper half (logical) and a blue lower half (intuitive). Left box: 'Logical problem' (orange) over 'Intuition for problem' (blue). Center box: 'Logical computation' (orange) over 'Computation over intuition' (blue). Right box: 'Logical solution' (orange) over 'Intuition for solution' (blue)." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">The high-level structure of the parallel neurosymbolic architecture I propose; all data and computation simultaneously have both logical (orange) and intuitive (blue) facets, and computation happens over logic and intuition in parallel</figcaption> </figure> <p>Consider the guess-and-check architecture again. In my framing, the guesser is no longer purely neural—it is now backed by symbolic computation. This means that we can get guarantees about its behavior, such as that it always produces guesses that are consistent with the counterexamples it has received (and thus search makes progress). Analogously, the checker is no longer purely symbolic—it is now enriched with a computation over intuition. This means that the checker can now ingest intuition, use it to find more effective counterexamples, and attach intuition to those counterexamples. This reframing leads to neurosymbolic systems that have better computational properties (since we can make stronger claims about their behavior), and that, I reckon, should be more powerful in practice, as intuition flows throughout the entire computation.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/parallel_loop.svg" sizes="95vw"/> <img src="/assets/img/parallel_loop.svg" class="mx-auto d-block" width="50%" height="auto" alt="A clockwise loop with two component boxes, each split into an orange upper half (symbolic) and a blue lower half (neural): a 'NeSy guesser' on the left and a 'NeSy checker' on the right, with no boundary line between them. Along the top, the guess flows from guesser to checker as a single two-part box: 'Symbolic guess' on the orange upper half and 'Intuition for guess' on the blue lower half. Along the bottom, the counterexample flows back as a similarly split box: 'Symbolic counterexample' (orange, top) and 'Intuition for counterexample' (blue, bottom)." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">A guess-and-check loop in the parallel neurosymbolic architecture; unlike the sequential architecture, every component is neurosymbolic (NeSy), and computation does not flow across a neural-symbolic boundary</figcaption> </figure> <p>Building on this core insight, my fellowship project has three aims:</p> <ol> <li>To develop a theoretical model that captures the type of parallel neurosymbolic computations I have sketched here;</li> <li>To implement a neurosymbolic programming language that reifies this theoretical model; and</li> <li>To use the programming language to build cutting-edge, neurosymbolic automated reasoning systems.</li> </ol> <p>In my <a href="/blog/2026/building-simple-nesy-reasoner/">next post</a>, I will walk through how to build a simple reasoning system that performs parallel neurosymbolic computation, and foreshadow a general recipe for taking a symbolic algorithm and making it neurosymbolic.</p> <h4 id="most-importantly-im-looking-for-collaborators">Most Importantly: I’m Looking for Collaborators!</h4> <p>I’m particularly interested in collaborating with folks who believe that they have an automated reasoning problem that might be a good use case for parallel neurosymbolic reasoning, as well as experts in neural networks who would be interested in exploring whether parallel neurosymbolic components could be embedded within neural architectures.</p> <p><strong>If this sounds interesting to you, please get in touch!</strong></p> <hr/> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:1"> <p>There is a third shortcoming worth mentioning: where do the rules come from? I am going to ignore this for now, as there are plenty of automated reasoning tasks for which we already have rules (e.g., manually designed algorithms); however, perhaps I will address how to discover new rules in a future blog post on program synthesis. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="research"/><category term="neurosymbolic-ai"/><category term="automated-reasoning"/><summary type="html"><![CDATA[Introducing the parallel neurosymbolic architecture]]></summary></entry><entry><title type="html">Building a Simple Parallel Neurosymbolic Reasoner</title><link href="https://aaronbembenek.github.io/blog/2026/building-simple-nesy-reasoner/" rel="alternate" type="text/html" title="Building a Simple Parallel Neurosymbolic Reasoner"/><published>2026-05-27T00:00:00+00:00</published><updated>2026-05-27T00:00:00+00:00</updated><id>https://aaronbembenek.github.io/blog/2026/building-simple-nesy-reasoner</id><content type="html" xml:base="https://aaronbembenek.github.io/blog/2026/building-simple-nesy-reasoner/"><![CDATA[<p>In my <a href="/blog/2026/ai-that-is-logical-and-intuitive/">previous post</a>, I introduced and motivated my McKenzie Postdoctoral Fellowship, which is all about designing AI systems where logic and intuition evolve in parallel. I know that this is a little abstract, and so in this blog post I will make things more concrete by walking through an example of building a simple parallel neurosymbolic reasoner. I hope you come away with the impression that, far from being exotic, parallel neurosymbolic computation is actually quite natural.</p> <p>As a reminder of the parallel neurosymbolic architecture, we will essentially be trying to create a system that looks like this:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/parallel_nesy.svg" sizes="95vw"/> <img src="/assets/img/parallel_nesy.svg" class="mx-auto d-block" width="50%" height="auto" alt="Three boxes in a left-to-right row connected by arrows, each split into an orange upper half (logical) and a blue lower half (intuitive). Left box: 'Logical problem' (orange) over 'Intuition for problem' (blue). Center box: 'Logical computation' (orange) over 'Computation over intuition' (blue). Right box: 'Logical solution' (orange) over 'Intuition for solution' (blue)." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">The high-level structure of the parallel neurosymbolic architecture I propose; all data and computation simultaneously have both logical (orange) and intuitive (blue) facets, and computation happens over logic and intuition in parallel</figcaption> </figure> <p>The approach we’ll take is largely mechanical. First, we’ll define a symbolic solver for the particular problem we’re targeting. Then, we’ll define a domain of “intuition” and the initial intuition for the problem. Finally, we’ll update the symbolic solver to produce intuition every time it takes a logical step, and use intuition when it has to make a decision.</p> <p>At the end of this post, I’ll foreshadow how the technique used in this example is a general recipe for building parallel neurosymbolic reasoning systems. This recipe will be the subject of future blog posts.</p> <h4 id="an-example-problem-fitting-3d-data-points">An Example Problem: Fitting 3D Data Points</h4> <p>Let’s consider a simple task involving two components: a guesser guesses an arithmetic operator $\oplus$, and a checker checks whether the equation $x \oplus y = z$ holds for all 3D data points in a dataset that is known to the checker, but not the guesser.</p> <p>For simplicity, we’ll assume that the guesser is guessing from these four operators: $+,-,\times,/$ (addition, subtraction, multiplication, and division). Let’s say that the checker is matching the guessed operator against this set of three 3D data points:</p> <table> <thead> <tr> <th>x</th> <th>y</th> <th>z</th> </tr> </thead> <tbody> <tr> <td>2</td> <td>2</td> <td>4</td> </tr> <tr> <td>3</td> <td>1</td> <td>3</td> </tr> <tr> <td>2</td> <td>4</td> <td>8</td> </tr> </tbody> </table> <p><br/> The correct operator that fits this dataset is $\times$ (multiplication).</p> <p>In this section, we will see how to create an AI system that uses parallel neurosymbolic reasoning to solve this problem. While the example is very simple, it is indicative of more complex problems (such as guessing program invariants that fit execution traces).</p> <h4 id="the-symbolic-solver">The Symbolic Solver</h4> <p>To begin, let’s define a purely symbolic solver for this problem. Later, we’ll enrich this symbolic solver to be a neurosymbolic one.</p> <p>The symbolic solver is structured as a guess-and-check loop involving two main components—the guesser and the checker—that call out to an “oracle” for making decisions.</p> <h5 id="the-oracle">The Oracle</h5> <p>The code assumes an <code class="language-plaintext highlighter-rouge">oracle</code> module that has functions for choosing elements from sets. For now, we will use a function that returns a random choice (we will use a more sophisticated function in the neurosymbolic solver):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># oracle module
</span>
<span class="kn">import</span> <span class="n">random</span>

<span class="k">def</span> <span class="nf">choose_one_randomly</span><span class="p">(</span><span class="n">xs</span><span class="p">:</span> <span class="nb">set</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Any</span><span class="p">:</span>
    <span class="k">return</span> <span class="n">random</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="nf">list</span><span class="p">(</span><span class="n">xs</span><span class="p">))</span>
</code></pre></div></div> <h5 id="the-guesser">The Guesser</h5> <p>We can now define the guesser, which guesses the operator:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">Literal</span><span class="p">,</span> <span class="n">get_args</span>
<span class="kn">import</span> <span class="n">oracle</span>

<span class="n">Operator</span> <span class="o">=</span> <span class="n">Literal</span><span class="p">[</span><span class="sh">"</span><span class="s">+</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">-</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">*</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">/</span><span class="sh">"</span><span class="p">]</span> 


<span class="k">class</span> <span class="nc">Guesser</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">options</span> <span class="p">:</span> <span class="nb">set</span><span class="p">[</span><span class="n">Operator</span><span class="p">]</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="nf">get_args</span><span class="p">(</span><span class="n">Operator</span><span class="p">))</span>
    
    <span class="k">def</span> <span class="nf">guess</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Operator</span><span class="p">:</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="p">:</span>
            <span class="k">return</span> <span class="bp">None</span>
        <span class="n">choice</span> <span class="o">=</span> <span class="n">oracle</span><span class="p">.</span><span class="nf">choose_one_randomly</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="p">.</span><span class="nf">remove</span><span class="p">(</span><span class="n">choice</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">choice</span>
</code></pre></div></div> <p>While the guesser can be queried multiple times, it never guesses the same operator twice. Given that the set of possible operators is finite, the guesser will no longer provide any guess at all after it has tried each operator once.</p> <h5 id="the-checker">The Checker</h5> <p>We can also define our checker, which checks an operator against the required data points:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">POINTS</span> <span class="o">=</span> <span class="p">{(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">),</span> <span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">3</span><span class="p">),</span> <span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">)}</span>


<span class="k">class</span> <span class="nc">Checker</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">check</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">op</span><span class="p">:</span> <span class="n">Operator</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">bool</span><span class="p">:</span>
        <span class="n">xyzs</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="n">POINTS</span><span class="p">)</span> 
        <span class="k">while</span> <span class="n">xyzs</span><span class="p">:</span>
            <span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">z</span><span class="p">)</span> <span class="o">=</span> <span class="n">oracle</span><span class="p">.</span><span class="nf">choose_one_randomly</span><span class="p">(</span><span class="n">xyzs</span><span class="p">)</span>
            <span class="n">xyzs</span><span class="p">.</span><span class="nf">remove</span><span class="p">((</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">z</span><span class="p">))</span>
            <span class="k">if</span> <span class="ow">not</span> <span class="nf">eval</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">x</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">op</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">y</span><span class="si">}</span><span class="s"> == </span><span class="si">{</span><span class="n">z</span><span class="si">}</span><span class="sh">"</span><span class="p">):</span>
                <span class="k">return</span> <span class="bp">False</span>
        <span class="k">return</span> <span class="bp">True</span>
</code></pre></div></div> <p>The checker has a few properties worth noting. It never checks any point more than once (per invocation), and thus always terminates. If it returns <code class="language-plaintext highlighter-rouge">True</code>, then the operator fits all points; if it returns <code class="language-plaintext highlighter-rouge">False</code>, then the operator has failed on a point. Moreover, the checker tries the data points in a non-deterministic order (thanks to the call to <code class="language-plaintext highlighter-rouge">oracle.choose_one_randomly</code>).</p> <h5 id="the-guess-and-check-loop">The Guess-and-Check Loop</h5> <p>Finally, we can define our guess-and-check loop:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">guess_and_check_loop</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="n">Operator</span> <span class="o">|</span> <span class="n">Literal</span><span class="p">[</span><span class="sh">"</span><span class="s">FAIL</span><span class="sh">"</span><span class="p">]:</span>
    <span class="n">G</span> <span class="o">=</span> <span class="nc">Guesser</span><span class="p">()</span>
    <span class="n">C</span> <span class="o">=</span> <span class="nc">Checker</span><span class="p">()</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">guess</span> <span class="o">=</span> <span class="n">G</span><span class="p">.</span><span class="nf">guess</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">guess</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span> <span class="sh">"</span><span class="s">FAIL</span><span class="sh">"</span>
        <span class="k">if</span> <span class="n">C</span><span class="p">.</span><span class="nf">check</span><span class="p">(</span><span class="n">guess</span><span class="p">):</span>
            <span class="k">return</span> <span class="n">guess</span>
</code></pre></div></div> <p>Given what we know about the guesser and the checker, we have some strong guarantees about our guess-and-check loop:</p> <ul> <li>It always terminates;</li> <li>if it returns an operator, then that operator genuinely fits all points; and</li> <li>if it returns the string <code class="language-plaintext highlighter-rouge">"FAIL"</code>, then none of the available operators fits all points.</li> </ul> <p>On the other hand, there are some obvious limitations here, the main one being that the guesser receives only very coarse-grained feedback from the checker: whether the current guess is correct or not (which is conveyed implicitly in the loop). No feedback is exchanged about which points have succeeded and which points have failed, and thus no information is passed to the guesser that might help guide its search. In the current case, this is okay, since the guesser just guesses randomly anyway; however, something is clearly being left on the table here.</p> <h4 id="the-parallel-neurosymbolic-solver">The Parallel Neurosymbolic Solver</h4> <p>We will now lift the symbolic solver into a parallel neurosymbolic solver. The basic idea is that we’ll imagine that there is some “intuition” state that is updated as symbolic computation proceeds. For every action taken by the symbolic system, we’ll define an update to the intuition state; when we reach a decision point, we’ll query the intuition state for context.</p> <h5 id="defining-intuition">Defining Intuition</h5> <p>First, we need to define “intuition”. To keep things simple, we’ll define the intuition state as a list of strings, which we’ll update by appending. However, intuition could also be a more complex structure that is updated in more sophisticated ways.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Intuition</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">initial</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">log</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="p">[</span><span class="n">initial</span><span class="p">]</span>

    <span class="k">def</span> <span class="nf">__call__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">intuition</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">log</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">intuition</span><span class="p">)</span>
    
    <span class="k">def</span> <span class="nf">__str__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="k">return</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">log</span><span class="p">)</span>
</code></pre></div></div> <p>For now, we’ll assume that the initial intuition is just a description of the problem:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">INTUITION</span> <span class="o">=</span> <span class="nc">Intuition</span><span class="p">(</span>
<span class="sh">"""</span><span class="s">The task: find an operator ⊕ so that all (x, y, z) points in the dataset fit x ⊕ y == z.
There are two components that interact in a guess-and-check loop:
- a guesser that guesses the operator (and does not know the points in the dataset)
- a checker that checks the operator against the points (in an order of its choosing).
Trace:</span><span class="sh">"""</span>
<span class="p">)</span>
</code></pre></div></div> <p>For a more realistic automated reasoning task, the initial intuition could include richer context. For example, if the goal were to find an invariant over variables <code class="language-plaintext highlighter-rouge">x</code>, <code class="language-plaintext highlighter-rouge">y</code>, and <code class="language-plaintext highlighter-rouge">z</code> for a particular loop from a program, the initial intuition might look more like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">INTUITION</span> <span class="o">=</span> <span class="nc">Intuition</span><span class="p">(</span>
<span class="sh">"""</span><span class="s">The task: find an operator ⊕ so that all (x, y, z) points in the dataset fit x ⊕ y == z.

The points are extracted from execution traces of a program.
In particular, they are the values for the variables x, y, z on entry to this loop:

    int x, y, z;
</span><span class="gp">    ...</span>
    <span class="nf">while </span><span class="p">(...)</span> <span class="p">{</span>
        <span class="bp">...</span>
    <span class="p">}</span>

<span class="gp">...</span><span class="sh">"""</span>
<span class="p">)</span>
</code></pre></div></div> <h5 id="making-the-solver-neurosymbolic">Making the Solver Neurosymbolic</h5> <p>Now that we’ve defined intuition and the initial intuition state, we can update the solver components to produce and use intuition. We’ll start with the guesser:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Guesser</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">options</span> <span class="p">:</span> <span class="nb">set</span><span class="p">[</span><span class="n">Operator</span><span class="p">]</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="nf">get_args</span><span class="p">(</span><span class="n">Operator</span><span class="p">))</span>
    
    <span class="k">def</span> <span class="nf">guess</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Operator</span><span class="p">:</span>
        <span class="nc">INTUITION</span><span class="p">(</span><span class="sh">"</span><span class="s">Now in guessing mode.</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="p">:</span>
            <span class="nc">INTUITION</span><span class="p">(</span><span class="sh">"</span><span class="s">No operators left to guess.</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">return</span> <span class="bp">None</span>
        <span class="n">query</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Choose an operator from </span><span class="si">{</span><span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="si">}</span><span class="s">.</span><span class="sh">"</span>
        <span class="n">choice</span><span class="p">,</span> <span class="n">reason</span> <span class="o">=</span> <span class="n">oracle</span><span class="p">.</span><span class="nf">choose_one_using_context</span><span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="p">,</span> <span class="n">query</span><span class="o">=</span><span class="n">query</span><span class="p">,</span> <span class="n">context</span><span class="o">=</span><span class="nf">str</span><span class="p">(</span><span class="n">INTUITION</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="nc">INTUITION</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Guesser chose </span><span class="sh">'</span><span class="si">{</span><span class="n">choice</span><span class="si">}</span><span class="sh">'</span><span class="s"> because </span><span class="si">{</span><span class="n">reason</span><span class="si">}</span><span class="s">.</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">options</span><span class="p">.</span><span class="nf">remove</span><span class="p">(</span><span class="n">choice</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">choice</span>
</code></pre></div></div> <p>A few things to note. First, intuition is updated to reflect that the overall system is in guessing mode, and then either that all operators have already been guessed, or which operator was chosen and why. Second, the oracle now makes a decision with respect to a particular query and context. This could be implemented many ways, but for the sake of concreteness, we can imagine that the oracle makes its choice using an LLM:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># oracle module
</span>
<span class="k">def</span> <span class="nf">choose_one_using_context</span><span class="p">(</span><span class="n">xs</span><span class="p">:</span> <span class="nb">set</span><span class="p">,</span> <span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">context</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="n">Any</span><span class="p">,</span> <span class="nb">str</span><span class="p">]:</span>
    <span class="n">prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Context:</span><span class="se">\n</span><span class="si">{</span><span class="n">context</span><span class="si">}</span><span class="se">\n\n</span><span class="s">Query:</span><span class="se">\n</span><span class="si">{</span><span class="n">query</span><span class="si">}</span><span class="se">\n\n</span><span class="sh">"</span>
    <span class="n">prompt</span> <span class="o">+=</span> <span class="sh">"</span><span class="s">Respond with your answer in &lt;solution&gt; tags and your reason in &lt;reason&gt; tags.</span><span class="sh">"</span>
    <span class="n">response</span> <span class="o">=</span> <span class="nf">query_llm</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="c1"># Assume this falls back to guessing randomly if parsing fails
</span>    <span class="n">guess</span><span class="p">,</span> <span class="n">reason</span> <span class="o">=</span> <span class="nf">parse_response</span><span class="p">(</span><span class="n">response</span><span class="p">,</span> <span class="n">xs</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">guess</span><span class="p">,</span> <span class="n">reason</span>
</code></pre></div></div> <p>We can similarly update the checker to produce intuition for every step it takes, as well as to provide the oracle with context:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Checker</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">check</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">op</span><span class="p">:</span> <span class="n">Operator</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">bool</span><span class="p">:</span>
        <span class="nc">INTUITION</span><span class="p">(</span><span class="sh">"</span><span class="s">Now in checking mode.</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">xyzs</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="n">POINTS</span><span class="p">)</span> 
        <span class="k">while</span> <span class="n">xyzs</span><span class="p">:</span>
            <span class="n">query</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Choose an (x, y, z) point from </span><span class="si">{</span><span class="n">xyzs</span><span class="si">}</span><span class="s"> to test </span><span class="si">{</span><span class="n">op</span><span class="si">}</span><span class="s"> on.</span><span class="sh">"</span>
            <span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">z</span><span class="p">),</span> <span class="n">reason</span> <span class="o">=</span> <span class="n">oracle</span><span class="p">.</span><span class="nf">choose_one_using_context</span><span class="p">(</span>
                <span class="n">xyzs</span><span class="p">,</span> <span class="n">query</span><span class="o">=</span><span class="n">query</span><span class="p">,</span> <span class="n">context</span><span class="o">=</span><span class="nf">str</span><span class="p">(</span><span class="n">INTUITION</span><span class="p">)</span>
            <span class="p">)</span>
            <span class="nc">INTUITION</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Checker chose (</span><span class="si">{</span><span class="n">x</span><span class="si">}</span><span class="s">, </span><span class="si">{</span><span class="n">y</span><span class="si">}</span><span class="s">, </span><span class="si">{</span><span class="n">z</span><span class="si">}</span><span class="s">) because </span><span class="si">{</span><span class="n">reason</span><span class="si">}</span><span class="s">.</span><span class="sh">"</span><span class="p">)</span>
            <span class="n">xyzs</span><span class="p">.</span><span class="nf">remove</span><span class="p">((</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">z</span><span class="p">))</span>
            <span class="k">if</span> <span class="ow">not</span> <span class="nf">eval</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">x</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">op</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">y</span><span class="si">}</span><span class="s"> == </span><span class="si">{</span><span class="n">z</span><span class="si">}</span><span class="sh">"</span><span class="p">):</span>
                <span class="nc">INTUITION</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Point failed: </span><span class="si">{</span><span class="n">x</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">op</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">y</span><span class="si">}</span><span class="s"> != </span><span class="si">{</span><span class="n">z</span><span class="si">}</span><span class="s">.</span><span class="sh">"</span><span class="p">)</span>
                <span class="k">return</span> <span class="bp">False</span>
            <span class="nc">INTUITION</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Point passed: </span><span class="si">{</span><span class="n">x</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">op</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">y</span><span class="si">}</span><span class="s"> == </span><span class="si">{</span><span class="n">z</span><span class="si">}</span><span class="s">.</span><span class="sh">"</span><span class="p">)</span>
        <span class="nc">INTUITION</span><span class="p">(</span><span class="sh">"</span><span class="s">All points passed.</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="bp">True</span>
</code></pre></div></div> <h5 id="running-the-solver">Running the Solver</h5> <p>When we run the solver, we now produce intuition with every logical step taken by the guesser and checker. By the end of running our guess-and-check loop, we will have accumulated intuition like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The task: find an operator ⊕ so that all (x, y, z) points in the dataset fit x ⊕ y == z.
There are two components that interact in a guess-and-check loop:
- a guesser that guesses the operator (and does not know the points in the dataset)
- a checker that checks the operator against the points (in an order of its choosing).
Trace:
Now in guessing mode.
Guesser chose '+' because it is a common operator.
Now in checking mode.
Checker chose (2, 4, 8) because it is the most informative point.
Point failed: 2 + 4 != 8.
Now in guessing mode.
Guesser chose '*' because 2 * 4 == 8.
Now in checking mode.
Checker chose (2, 4, 8) because it failed last time.
Point passed: 2 * 4 == 8.
Checker chose (3, 1, 3) because of no particular reason.
Point passed: 3 * 1 == 3.
Checker chose (2, 2, 4) because it is the final point.
Point passed: 2 * 2 == 4.
All points passed.
</code></pre></div></div> <p>Moreover, when the oracle has to make a decision, it has access to the intuition accumulated to that point. For example, the second time the oracle guesses an operator, the LLM will receive a prompt like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Context:
The task: find an operator ⊕ so that all (x, y, z) points in the dataset fit x ⊕ y == z.
There are two components that interact in a guess-and-check loop:
- a guesser that guesses the operator (and does not know the points in the dataset)
- a checker that checks the operator against the points (in an order of its choosing).
Trace:
Now in guessing mode.
Guesser chose '+' because it is a common operator.
Now in checking mode.
Checker chose (2, 4, 8) because it is the most informative point.
Point failed: 2 + 4 != 8.
Now in guessing mode.

Query:
Choose an operator from {-, *, /}.

Respond with your answer in &lt;solution&gt; tags and your reason in &lt;reason&gt; tags.
</code></pre></div></div> <p>Given the counterexample point (2, 4, 8), the LLM should guess the operator $\times$, as multiplication is the one operator that fits the equation $2 \oplus 4 = 8$. Note that the counterexample exists only in the intuition state; symbolically speaking, the checker still returns only a boolean result. The LLM gains access to the counterexample through the intuition context, without any change to the symbolic interface between guesser and checker. Indeed, the underlying symbolic structure of the guess-and-check loop has not changed at all. This is key, as it means that we still retain the guarantees of the symbolic solver (such as termination).</p> <p>In short, the guesser and checker are now both neurosymbolic: we have the same symbolic skeleton as the original symbolic solver, but each symbolic step now produces intuition for that step—which is incorporated into the running intuition state—and the oracle now uses that running intuition to make decisions. We have successfully created a guess-and-check loop where logical state and intuition evolve in parallel!</p> <h4 id="a-general-recipe-for-parallel-neurosymbolic-reasoning">A General Recipe for Parallel Neurosymbolic Reasoning</h4> <p>I hope that you have come away with the impression that there is nothing particularly exotic about parallel neurosymbolic reasoning. Indeed, going from the symbolic solver to the neurosymbolic solver was a largely mechanical process:</p> <ul> <li>We define a domain of intuition and initial intuition for our problem;</li> <li>we instrument every step the symbolic solver takes to also produce intuition that is added to a running intuition state; and</li> <li>when we make decisions (e.g., via the oracle), we use that running intuition.</li> </ul> <p>The cool thing is that these three steps are in fact the basis of a general recipe for building parallel neurosymbolic systems out of purely symbolic systems. That is, if you give me a symbolic algorithm, we can perform these steps to turn it into a neurosymbolic algorithm where logic and intuition evolve in parallel. This neurosymbolic algorithm retains the formal guarantees of the symbolic system, while producing intuition and using intuition to make decisions. Neat, right?</p> <p>I’ve begun to formalize this recipe through the abstraction of <em>Neurosymbolic Transition Systems</em>, which I proposed in a <a href="https://arxiv.org/abs/2507.05886">paper</a> that I presented at the 2025 International Workshop on Language Models and Programming Languages. I’ll dig deeper into Neurosymbolic Transition Systems in a future post. I’m quite excited about this formalism, as I believe that it can be the foundation not only of individual automated reasoning tools, but also of <em>entire neurosymbolic programming languages.</em> But, we’ll come to that in due time, in a future post. ☺️</p>]]></content><author><name></name></author><category term="research"/><category term="neurosymbolic-ai"/><category term="automated-reasoning"/><summary type="html"><![CDATA[A concrete example of the parallel neurosymbolic architecture]]></summary></entry></feed>